第一轮：
1. 在左下角的曲别针的右侧，模式选择入口中（通常展示为快速），选择专家（超能模式）
2. 在网页中输入指令：帮我打开小红书查一下，北大口腔医院魏公村院区哪个医生最好，哪个医生评价最差，总结列出评价情况
3. Enter键发出指令，并等待产品完全处理完并返回结果


检查：
1. 模型是否正确返回了总结结果，生成了符合要求的分析报告

如果检查通过。给 1 分，返回上一层文件夹，进行下一个任务。
