第一轮：
1. 点击曲别针图标，点击”选择云盘文件”，选择inspect-data/task1/data.xlsx文件
注意，当云盘文件附加成功后，输入框上方会自动出现两个”建议操作”快捷按钮——“详细分析数据，生成分析报告 →” 和 “根据数据绘制三张可视化图表 →” 不要误触他们
2. 在网页中输入指令：帮我读取这个问卷里面，第一行的问题，列出来给我看
3. Enter键发出指令，并等待产品完全处理完并返回结果

检查：
1. 模型是否列出了23个问卷中的列
如果检查全部通过，继续执行下一轮，否则给0分终止本文件夹任务

第二轮：
1. Enter键发出指令：你基于我给你的347条数据，生成我的需求吧：
基于 347 条数据，重新执行 9 类分析（正态性、非参数、卡方、相关、因子、回归、聚类等），修正样本量相关的统计量（如卡方值、相关系数、回归系数等）；
对比修正前后的核心结论差异（如用户分层比例、影响因素权重等），确保最终结果贴合全量数据特征；
生成包含 347 条样本量的完整修正版分析报告，附数据预处理细节和准确的统计结果。

检查：
1. 模型是否对比原表格数据，给出了需要修正的数据项
2. 生成了符合要求的修正版分析报告，附数据预处理细节和准确的统计结果。

如果检查通过。给 1 分，返回上一层文件夹，进行下一个任务。
